import { Vec2 } from "cc";

export namespace util {
    /**
 * 节点坐标转换
 * @param node 欲要转换的节点
 * @param newParent 新父节点
 */
    // export function convertToParent(
    //     node: Node,
    //     newParent: Node,
    //     offset?: Vec2
    // ): Vec2 {
    //     const worldPos = node. convertToWorldSpaceAR(offset || cc.v2(0, 0));
    //     return newParent.convertToNodeSpaceAR(worldPos);
    // }

}
